package com.auction;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertTrue;

class SmokeTest {
    @Test
    void smokeTest() {
        // Test đơn giản nhất — chỉ để CI có gì chạy
        assertTrue(true, "Pipeline hoạt động!");
    }
}
